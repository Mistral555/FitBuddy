<nav class="navbar navbar-expand-lg fixed-top">
	<div class="container">

		<a class="navbar-brand" href="{{path('home')}}">FitBuddy</a>

		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav ml-lg-auto">

				{% if app.user %}
					<li class="nav-item">
						<a href="{{path('home')}}" class="nav-link smoothScroll">Accueil</a>
					</li>
					<li class="nav-item">
						<a href="{{path('profile')}}" class="nav-link smoothScroll">Mon profil</a>
					</li>
					<li class="nav-item">
						<a href="{{path('app_logout')}}" class="nav-link smoothScroll">Déconnexion</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a href="{{path('home')}}" class="nav-link smoothScroll">Accueil</a>
					</li>
					<li class="nav-item">
						<a href="{{path('home')}}#about" class="nav-link smoothScroll">A propos</a>
					</li>
					<li class="nav-item">
						<a href="{{path('home')}}#class" class="nav-link smoothScroll">Programmes</a>
					</li>
					<li class="nav-item">
						<a href="{{path('home')}}#contact" class="nav-link smoothScroll">Contact</a>
					</li>
					<li class="nav-item">
						<a href="{{path('app_login')}}" class="nav-link smoothScroll">Connexion</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
